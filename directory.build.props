<Project>
  <!-- Import modularised warning configurations -->
  <Import Project="$(MSBuildThisFileDirectory)warnings.build.props" />

  <PropertyGroup>
    <!-- Build settings -->
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>
</Project>